<link rel="stylesheet" href="/stylesheets/topics.css">
<div id="main">
    {{#if news}}
    <div id="topic-title">
        <img src="/images/regions/{{news.0.region}}.jpg" id="topic-title-pic" alt="">
        <span>{{news.0.region}}</span>
    </div>
    <section id="news-section">
        <div class="container">
            <div class="row">
                {{# each news}}
                <div class=" col-sm-10 col-lg-5 news">
                    <div class="photo">
                        <img src="/images/articles/{{this.id}}.jpg" alt="" class="img-responsive">
                        {{#if this.subtopic}}
                            <a class="href-on-photo" href="article?id={{this.id}}">{{this.subtopic}}</a>
                        {{else}}
                            <a class="href-on-photo" href="article?id={{this.id}}">останні</a>
                        {{/if}}
                    </div>
                    <h1>{{this.title}}</h1>
                    <hr>
                    <a href="article?id={{this.id}}">
                    <img src="/images/label/external-link-alt-solid.svg" id="external-link"
                         class="external-link-alt-solid text-right">
                    <p>{{this.description}}</p>
                    <p class="article-author font-italic" >{{this.author}}</p>
                    </a>
                </div>
                </a>
                {{/each}}
            </div>

         </div>
    </section>
    {{else}}
        <br>
        <br>
        <h1>На даний момент ми не маємо статей в цьому регіоні</h1>
        <br>
        <p>Але вони обов'язково з'являться.</p>
        <span style="float: right">З повагою <i>Аналітичний центр Ad Astra</i></span>
    {{/if}}
</div>
